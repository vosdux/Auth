import React from 'react';

import './Spinner.scss';

const Spinner = () => {
    return (
        <div className="spin-wrapper">
            <div className="spinner" />
        </div>
    );
};

export default Spinner;
